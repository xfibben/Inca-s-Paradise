---
interface Props {
  tipos: any[];
  publicStrapiUrl: string;
}

const { tipos, publicStrapiUrl } = Astro.props;

function absUrl(raw: string): string {
  if (!raw) return '';
  return raw.startsWith('http') ? raw : `${publicStrapiUrl}${raw}`;
}

function getCardImage(media: any): { src: string; srcset: string } {
  if (!media) return { src: '', srcset: '' };
  const small  = absUrl(media?.formats?.small?.url  ?? '');
  const medium = absUrl(media?.formats?.medium?.url ?? '');
  const src    = small || medium || absUrl(media?.url ?? '');
  const srcset = [
    small  ? `${small} 500w`  : '',
    medium ? `${medium} 750w` : '',
  ].filter(Boolean).join(', ');
  return { src, srcset };
}
---

<!-- Sección grid de tipos de transporte -->
<section class="py-20 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">

    <!-- Encabezado de sección -->
    <div class="text-center mb-16">
      <p class="text-amber-600 uppercase tracking-widest text-sm font-medium mb-3">Movilidad</p>
      <h2
        class="text-4xl md:text-5xl text-stone-800"
        style="font-family: 'Playfair Display', serif; font-style: italic;"
      >
        Nuestros Transportes
      </h2>
    </div>

    <!-- Grid 4 columnas -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
      {tipos.map((tipo) => {
        const { src: imgSrc, srcset: imgSrcset } = getCardImage(tipo.image);

        return (
          <article class="group relative rounded-2xl overflow-hidden bg-stone-100 shadow-md hover:shadow-xl transition-shadow duration-300 min-h-105 flex flex-col">

            <!-- Imagen de fondo -->
            {imgSrc ? (
              <img
                src={imgSrc}
                srcset={imgSrcset || undefined}
                sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 25vw"
                alt={tipo.nombre}
                class="absolute inset-0 w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
                loading="lazy"
              />
            ) : (
              <div class="absolute inset-0 bg-linear-to-br from-stone-300 to-stone-500" />
            )}

            <!-- Overlay degradado -->
            <div class="absolute inset-0 bg-linear-to-t from-black/75 via-black/20 to-transparent" />

            <!-- Título centrado al fondo -->
            <div class="relative mt-auto p-6 text-white text-center">
              <h3
                class="text-2xl font-medium leading-snug"
                style="font-family: 'Playfair Display', serif; font-style: italic;"
              >
                {tipo.nombre}
              </h3>
            </div>

          </article>
        );
      })}

      <!-- Placeholders si hay menos de 4 tipos en el backend -->
      {Array.from({ length: Math.max(0, 4 - tipos.length) }).map(() => (
        <article class="relative rounded-2xl overflow-hidden bg-stone-100 shadow-md min-h-105 flex flex-col">
          <div class="absolute inset-0 bg-linear-to-br from-stone-200 to-stone-400" />
          <div class="relative mt-auto p-6 text-white/50 text-center">
            <h3
              class="text-2xl font-medium"
              style="font-family: 'Playfair Display', serif; font-style: italic;"
            >
              Próximamente
            </h3>
          </div>
        </article>
      ))}
    </div>

  </div>
</section>
